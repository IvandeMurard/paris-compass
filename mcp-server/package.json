{
  "name": "@paris-compass/mcp-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server exposing Compass's scoring core to an agent — the same trust boundary as an anonymous visitor, never a privileged connection.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@supabase/supabase-js": "^2.112.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "tsx": "^4.19.2",
    "typescript": "^5.5.3"
  }
}
